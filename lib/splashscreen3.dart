import 'package:flutter/material.dart';

class Splashscreen3 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text("Splashscreen3"),
      ),
      body: Center(
        child: Text("Tingkatkan Kompetensi,\nDimanapun"),
      ),
    );
  }

}